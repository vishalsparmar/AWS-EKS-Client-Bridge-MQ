kc get pods -n kong-external -o wide | awk 'NR>1 && $7 != "<none>" {print $7}' | while read node; do
  kc get node "$node" -o jsonpath='{.metadata.labels.topology\.kubernetes\.io/zone}' 2>/dev/null
done > /tmp/azs.txt && sort /tmp/azs.txt | uniq -c && rm -f /tmp/azs.txt
