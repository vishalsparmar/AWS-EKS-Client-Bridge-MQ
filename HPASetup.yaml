kubectl get pods -n <namespace> -o wide | awk 'NR>1{print $7}' | grep -v '<none>' | xargs -I {} kubectl get node {} -o jsonpath='{.metadata.labels.topology\.kubernetes\.io/zone}' | sort | uniq -c
For All Namespaces (without jq):

bash
kubectl get pods -A -o wide | awk 'NR>1{print $7}' | grep -v '<none>' | xargs -I {} kubectl get node {} -o jsonpath='{.metadata.labels.topology\.kubernetes\.io/zone}' | sort | uniq -c
Sample Output:
